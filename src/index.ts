// Public entry for the clotho editor.
//
// Populated by the port described in docs/PORTING.md. The order there matters: the
// state layer is the foundation everything else sits on, and it is the piece that has
// to change for v1's parentId grouping and asset registry.

export const VERSION = '0.0.0';
